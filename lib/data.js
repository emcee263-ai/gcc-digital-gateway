import fs from "fs";import path from "path";const root=path.join(process.cwd(),"data");export function read(n){return JSON.parse(fs.readFileSync(path.join(root,n),"utf8"))}export function write(n,d){fs.writeFileSync(path.join(root,n),JSON.stringify(d,null,2))}