 "use client";import {useApp} from "@/components/AppProvider";import Shell from "@/components/Shell";import Guard from "@/components/Guard";import {ShieldCheck} from "lucide-react";
export default function Profile(){const{user}=useApp();return <Guard roles={["resident"]}><Shell title="Profile"><div className="content"><h1 style={{fontFamily:"Space Grotesk"}}>My Profile</h1><div className="card" style={{maxWidth:700}}><h2>{user?.name}</h2><p className="muted">{user?.email}</p><hr style={{border:0,borderTop:"1px solid #e2e8eb"}}/><p><b>Account:</b> {user?.account}</p><p><b>Address:</b> {user?.address}</p><p className="small muted"><ShieldCheck size={13}/> Information shown according to role permissions.</p></div></div></Shell></Guard>}