 "use client";import {useState} from "react";import {useApp} from "@/components/AppProvider";import Guard from "@/components/Guard";import Shell from "@/components/Shell";import {Send} from "lucide-react";import {useRouter,useSearchParams} from "next/navigation";
export default function NewRequest(){const{user}=useApp(),r=useRouter(),sp=useSearchParams(),[f,setF]=useState({category:sp.get("category")||"Water",location:"",description:""}),[busy,setBusy]=useState(false);async function go(e){e.preventDefault();setBusy(true);let z=await fetch("/api/requests",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...f,resident:user.id})});let x=await z.json();alert("Request submitted: "+x.id);r.push("/requests")}return <Guard roles={["resident"]}><Shell title="New Service Request"><div className="content"><div className="eyebrow">Report a municipal problem</div><h1 style={{fontFamily:"Space Grotesk"}}>Submit a service request</h1><p className="muted">Your request will receive a reference number and follow the Council workflow.</p><form className="card form" onSubmit={go}><div className="fields"><div className="field"><label>Service category</label><select value={f.category} onChange={e=>setF({...f,category:e.target.value})}>{["Water","Sewer","Roads","Waste","Street lighting","Drainage","Public facilities","Other"].map(x=><option key={x}>{x}</option>)}</select></div><div className="field"><label>Location</label><input required value={f.location} placeholder="Area / address" onChange={e=>setF({...f,location:e.target.value})}/></div><div className="field full"><label>Description</label><textarea required value={f.description} placeholder="Describe the problem..." onChange={e=>setF({...f,description:e.target.value})}/></div></div><div style={{textAlign:"right"}}><button disabled={busy} className="btn primary"><Send size={15}/>{busy?"Submitting...":"Submit request"}</button></div></form></div></Shell></Guard>}