 "use client";import {useApp} from "@/components/AppProvider";import Guard from "@/components/Guard";import Shell from "@/components/Shell";import {Plus} from "lucide-react";const B=({s})=><span className={"badge "+s}>{s.replace("_"," ").toUpperCase()}</span>;
export default function Requests(){const{user,requests}=useApp(),mine=requests.filter(x=>x.resident===user?.id);return <Guard roles={["resident"]}><Shell title="My Requests"><div className="content"><div style={{display:"flex",justifyContent:"space-between",gap:10,alignItems:"center"}}><div><div className="eyebrow">Service Requests</div><h1 style={{fontFamily:"Space Grotesk"}}>Track your requests.</h1></div><button className="btn primary" onClick={()=>location.href="/requests/new"}><Plus size={15}/> New request</button></div><div className="card tablewrap" style={{marginTop:20}}><table className="table"><thead><tr><th>Reference</th><th>Issue</th><th>Department</th><th>Date</th><th>Status</th></tr></thead><tbody>{mine.map(x=><tr key={x.id}><td><b>{x.id}</b></td><td>{x.category}<div className="small muted">{x.location}</div></td><td>{x.department}</td><td>{x.createdAt}</td><td><B s={x.status}/></td></tr>)}</tbody></table></div></div></Shell></Guard>}