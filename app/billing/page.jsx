 "use client";import {useEffect,useState} from "react";import {useApp} from "@/components/AppProvider";import Shell from "@/components/Shell";import Guard from "@/components/Guard";import {Wallet,Download} from "lucide-react";
export default function Billing(){const{user}=useApp(),[b,setB]=useState([]);useEffect(()=>{fetch("/api/bills?account="+user?.account).then(x=>x.json()).then(setB)},[user]);return <Guard roles={["resident"]}><Shell title="Billing"><div className="content"><div className="hero"><div><div className="eyebrow">Municipal Billing</div><h1>Your bills & statements.</h1><p>Payment processing is represented as a prototype.</p></div><div className="hero-side"><Wallet/><div className="small">AMOUNT DUE</div><h2>ZWL {b[0]?.amount}</h2></div></div><h2 className="section">Billing history</h2><div className="card tablewrap"><table className="table"><thead><tr><th>Period</th><th>Amount</th><th>Due</th><th>Status</th><th></th></tr></thead><tbody>{b.map(x=><tr key={x.id}><td><b>{x.period}</b></td><td>ZWL {x.amount}</td><td>{x.due}</td><td>{x.status}</td><td><button className="btn light"><Download size={14}/> Statement</button></td></tr>)}</tbody></table></div></div></Shell></Guard>}