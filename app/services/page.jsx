 "use client";import {useEffect,useState} from "react";import Shell from "@/components/Shell";import Guard from "@/components/Guard";import {Search,ArrowRight} from "lucide-react";
export default function Services(){const[a,setA]=useState([]),[q,setQ]=useState("");useEffect(()=>{fetch("/api/departments").then(x=>x.json()).then(setA)},[]);let f=a.filter(x=>(x.name+x.description).toLowerCase().includes(q.toLowerCase()));return <Guard roles={["resident"]}><Shell title="Services"><div className="content"><div className="hero"><div><div className="eyebrow">Service Directory</div><h1>Find a Council service.</h1><p>Search departments and discover the right municipal service.</p></div></div><div className="toolbar"><Search size={18}/><input className="search" placeholder="Search services..." value={q} onChange={e=>setQ(e.target.value)}/></div><div className="grid3">{f.map(x=><div className="card" key={x.name}><div style={{display:"flex",justifyContent:"space-between"}}><Search color="#08705f"/><ArrowRight size={16}/></div><h3>{x.name}</h3><p className="muted">{x.description}</p><button className="btn light" onClick={()=>location.href="/requests/new?category="+x.name}>Report an issue</button></div>)}</div></div></Shell></Guard>}