 "use client";import {useApp} from "@/components/AppProvider";import Shell from "@/components/Shell";import Guard from "@/components/Guard";import {Newspaper,ShieldCheck} from "lucide-react";
export default function Notices(){const{notices}=useApp();return <Guard roles={["resident"]}><Shell title="Council Notices"><div className="content"><div className="hero"><div><div className="eyebrow">Official Information</div><h1>Council notices & updates.</h1><p>Stay informed through one source for official municipal information.</p></div></div><div className="grid2" style={{marginTop:20}}>{notices.map(n=><article className="card" key={n.id}><div style={{display:"flex",justifyContent:"space-between"}}><span className="badge resolved">{n.category}</span><ShieldCheck size={17} color="#08705f"/></div><h2 style={{fontFamily:"Space Grotesk"}}>{n.title}</h2><p className="muted">{n.body}</p><span className="small muted">{n.date}</span></article>)}</div></div></Shell></Guard>}