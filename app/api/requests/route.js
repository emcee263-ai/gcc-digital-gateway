import {NextResponse} from "next/server";import {read,write} from "@/lib/data";export async function GET(){return NextResponse.json(read("requests.json"))}export async function POST(req){let b=await req.json(),d=read("requests.json"),n=String(483+d.length).padStart(5,"0"),x={id:"GCC-2026-"+n,...b,department:b.category==="Water"?"Water":b.category==="Waste"?"Waste":"Engineering",status:"submitted",createdAt:new Date().toISOString().slice(0,10),assignedTo:""};d.unshift(x);write("requests.json",d);return NextResponse.json(x)}