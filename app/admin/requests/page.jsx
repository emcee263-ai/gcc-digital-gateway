 "use client";import {useApp} from "@/components/AppProvider";import Shell from "@/components/Shell";import Guard from "@/components/Guard";import {useState} from "react";const B=({s})=><span className={"badge "+s}>{s.replace("_"," ").toUpperCase()}</span>;
export default function AR(){const{requests,setRequests}=useApp(),[q,setQ]=useState("");async function upd(id,status){await fetch("/api/requests/"+id,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status,assignedTo:status==="submitted"?"":"Council Service Team"})});setRequests(requests.map(x=>x.id===id?{...x,status}:x))}let f=requests.filter(x=>(x.id+x.category+x.location).toLowerCase().includes(q.toLowerCase()));return <Guard roles={["admin","clerk"]}><Shell title="Request Management"><div className="content"><div className="eyebrow">Council Workflow</div><h1 style={{fontFamily:"Space Grotesk"}}>Service requests</h1><input className="search" placeholder="Search requests..." value={q} onChange={e=>setQ(e.target.value)}/><div className="card tablewrap" style={{marginTop:18}}><table className="table"><thead><tr><th>Reference</th><th>Issue</th><th>Location</th><th>Department</th><th>Status</th><th>Update</th></tr></thead><tbody>{f.map(x=><tr key={x.id}><td><b>{x.id}</b></td><td>{x.category}<div className="small muted">{x.description}</div></td><td>{x.location}</td><td>{x.department}</td><td><B s={x.status}/></td><td><select value={x.status} onChange={e=>upd(x.id,e.target.value)}><option value="submitted">Submitted</option><option value="assigned">Assigned</option><option value="in_progress">In Progress</option><option value="resolved">Resolved</option></select></td></tr>)}</tbody></table></div></div></Shell></Guard>}