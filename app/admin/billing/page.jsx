 "use client";import {useEffect,useState} from "react";import Shell from "@/components/Shell";import Guard from "@/components/Guard";import {Wallet} from "lucide-react";
export default function AB(){const[b,setB]=useState([]);useEffect(()=>{fetch("/api/bills").then(x=>x.json()).then(setB)},[]);return <Guard roles={["admin","finance"]}><Shell title="Billing Administration"><div className="content"><div className="hero"><div><div className="eyebrow">Finance</div><h1>Billing overview.</h1><p>Prototype view of municipal billing records.</p></div><div className="hero-side"><Wallet/><h2>{b.length}</h2><span className="small">billing records</span></div></div><div className="card tablewrap" style={{marginTop:20}}><table className="table"><thead><tr><th>Account</th><th>Period</th><th>Amount</th><th>Due</th><th>Status</th></tr></thead><tbody>{b.map(x=><tr key={x.id}><td>{x.account}</td><td>{x.period}</td><td>{x.currency} {x.amountDue ?? x.amount}</td><td>{x.due}</td><td>{x.status}</td></tr>)}</tbody></table></div></div></Shell></Guard>}